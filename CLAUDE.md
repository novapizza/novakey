# NovaKey — Project Instructions

## Pre-commit requirements

- **Always run a code review with the `/security-review` skill before committing code.** Complete the security review of the pending changes and address (or explicitly surface) any findings first — never commit without it.
